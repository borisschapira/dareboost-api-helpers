{
  "scripts": {
    "preoutput-tracking-conf": "echo 'Cleaning old json and csv files…' && rimraf output.json && rimraf output.csv",
    "output-tracking-conf": "node output-tracking-conf.js",
    "postoutput-tracking-conf": "echo 'Convert file to output.csv' && json2csv -i output.json -o output.csv -d '\t'",
    "lint": "xo",
    "test": "xo"
  },
  "name": "collect-trackings",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "xo": "^0.21.1"
  },
  "dependencies": {
    "foreach-timeout": "^2.0.2",
    "json2csv": "^4.1.3",
    "request": "^2.87.0",
    "request-promise": "^4.2.2",
    "rimraf": "^2.6.2",
    "underscore": "^1.9.1"
  }
}
